import { Property } from "@/types/property";

// Este arquivo agora é usado apenas como fallback
// Os dados reais vêm do PropertyService
export const featuredProperties: Property[] = [];

export const allProperties: Property[] = [];
